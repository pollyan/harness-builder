#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
Commit created.
Before pushing, the pre-push hook will run full local regression tests, including DeepSeek acceptance.
Push no longer waits for GitHub Actions in the Codex workflow.
MSG
