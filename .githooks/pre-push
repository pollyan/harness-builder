#!/usr/bin/env bash
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

echo "Running full regression tests before push..."
scripts/test-full.sh

cat <<'MSG'
Local full regression passed.
Push may proceed; GitHub Actions are not checked as a blocking local step.
MSG
